/* describe('Visual Component', () => {
  beforeEach(() => cy.visit('/iframe.html?id=visualcomponent--primary'));
  it('should render the component', () => {
    cy.get('aia-web-visual').should('exist');
  });
});
*/
